h1. About

p. Simple concept of a now.js application, pretending to finally become a videogame about miners.

h1. Dependencies

p. Mine.js depends on SocketStream ( https://github.com/socketstream/socketstream ), install it first!

h1. How To

p. To start simply execute 
<pre>socketstream start</pre>

and go to

<pre>
http://localhost:3000
</pre>
